package domain

import "time"

// Order represents the Orders module's aggregate root.
type Order struct {
	ID          string
	CustomerID  string
	Status      string
	TotalAmount float64
	CreatedAt   time.Time
}
