package domain

import "time"

// Order is the microservice's primary aggregate within the order module.
type Order struct {
	ID          string
	CustomerID  string
	Status      string
	TotalAmount float64
	CreatedAt   time.Time
}
