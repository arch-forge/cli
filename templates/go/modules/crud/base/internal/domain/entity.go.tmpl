package domain

import "time"

// {{ index .Options "entity_name" | default "Entity" }} represents the {{ index .Options "entity_name" | default "Entity" }} aggregate root.
type {{ index .Options "entity_name" | default "Entity" }} struct {
	ID        string    `json:"id" db:"id"`
	CreatedAt time.Time `json:"created_at" db:"created_at"`
	UpdatedAt time.Time `json:"updated_at" db:"updated_at"`
}

// New{{ index .Options "entity_name" | default "Entity" }} creates a new {{ index .Options "entity_name" | default "Entity" }} with generated ID.
func New{{ index .Options "entity_name" | default "Entity" }}() *{{ index .Options "entity_name" | default "Entity" }} {
	return &{{ index .Options "entity_name" | default "Entity" }}{
		ID:        newID(),
		CreatedAt: time.Now().UTC(),
		UpdatedAt: time.Now().UTC(),
	}
}
