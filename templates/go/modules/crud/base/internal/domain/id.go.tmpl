package domain

import (
	"crypto/rand"
	"encoding/hex"
)

// newID generates a random 16-byte hex ID.
func newID() string {
	b := make([]byte, 16)
	_, _ = rand.Read(b)
	return hex.EncodeToString(b)
}
