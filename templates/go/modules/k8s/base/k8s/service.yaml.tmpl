apiVersion: v1
kind: Service
metadata:
  name: {{ .Project.Name }}
  namespace: {{ index .Options "namespace" | default "default" }}
spec:
  selector:
    app: {{ .Project.Name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
